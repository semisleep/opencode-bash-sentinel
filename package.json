{
  "name": "opencode-bash-sentinel",
  "version": "0.1.0",
  "description": "Fail-closed AST-based Bash permission helper for OpenCode with workspace, external-path, and exact-profile policies.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "index.ts",
    "README.md",
    "NOTICE.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0.73",
    "bun-types": "^1.4.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "opencode": ">=1.18.0 <2.0.0"
  }
}
